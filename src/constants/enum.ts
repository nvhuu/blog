export enum ApiUrl {
  BLOGS = '/blogs',
  BLOG_ID = '/blogs/{id}'
}

export enum STATUS {
  SUCCESS = 'success',
  WARNING = 'warning',
  ERROR = 'error',
  INFO = 'info'
}
export enum HTTP_CODE {
  NOT_FOUND = 404,
  SUCCESS = 200,
  CREATED = 201,
  SERVER_ERROR = 500
}
